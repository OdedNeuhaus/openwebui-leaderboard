1. Access the leaderboard:
{{- if .Values.ingress.enabled }}
   https://{{ (index .Values.ingress.hosts 0).host }}
{{- else }}
   kubectl port-forward svc/{{ include "openwebui-leaderboard.fullname" . }} 8080:{{ .Values.service.port }}
{{- end }}

2. Make sure the chart points to the same OpenWebUI database used by your real instance.
